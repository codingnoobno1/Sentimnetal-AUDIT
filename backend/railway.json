{
  "build": {
    "builder": "nixpacks",
    "config": {
      "nixpacksPythonVersion": "3.11"
    }
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckInterval": 30
  }
}
